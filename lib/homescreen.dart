import 'package:flutter/material.dart';

class HomeScreenHiro extends StatelessWidget {
  const HomeScreenHiro({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
